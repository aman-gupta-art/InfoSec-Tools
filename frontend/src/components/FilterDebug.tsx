import React from 'react';

const FilterDebug: React.FC = () => {
  return (
    <div className="hidden">
      {/* Debug component hidden by default */}
    </div>
  );
};

export default FilterDebug; 